<%- include('partials/header') %>
    <%- include('partials/nav') %>

    <main class="container">
        <div id="account">
            <div class="avatar">
                <img src="/images.profile_pictures/<%= user.picture %>" alt="">
            </div>
            <h3><%= user.username %></h3>
            <p><%= user.gender %></p>
            <p><%= user.location %></p>

            <% if (user.client_id === userID) { %>
                <a href="/edit-account" id="btn-edit">Edit Account</a>
                <div id="form">
                    <!-- <form action="/edit-account/<%= user.client_id %>" method="post" enctype="multipart/form-data">
                        <label for="">Full Name</label>
                        <input type="text" name="fullname" id="" value="<%= user.fullname %>">
                        <label for="">Email Address</label>
                        <input type="email" name="email" id="" value="<%= user.email %>">
                        <label for="">Phone number</label>
                        <input type="text" name="phonenumber" value="<%= user.phonenumber %>">
                        <label for="">Gender</label>
                        <select name="gender" id="">
                            <% if(user.gender !== null) { %>
                                <option value="<%= user.gender %>"><%= user.gender %></option>
                            <% } %>
                            <option>-- Choose Gender --</option>
                            <option value="Male">Male</option>
                            <option value="Female">Female</option>
                        </select>
                        <label for="">Location</label>
                        <input type="text" name="location" id="" value="<%= user.location %>">
                        <label for="">Profile Picture</label>
                        <input type="file" name="picture" id="">
                        <label for="">Password</label>
                        <input type="password" name="password" id="">
                        <div>
                            <button class="btn" id="btn-cancel">Cancel</button>
                            <input class="btn" type="submit" value="update">
                        </div> 
                    </form>
                </div> -->

            <% } %>    
        </div>
    </main>

    <!-- <script>
        let editBtn = document.querySelector('#btn-edit')
        let cancelBtn = document.querySelector('#btn-cancel')
        let form = document.querySelector('#form')

        editBtn.addEventListener('click', (e) => {
            e.preventDefault()
            form.style.display = 'block'
        })

        cancelBtn.addEventListener('click', (e) => {
            e.preventDefault()
            form.style.display = 'none'
        })
    </script> -->


<%- include('partials/footer') %>